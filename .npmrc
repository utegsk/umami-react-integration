# .npmrc
# ---
# scopes all @utegsk/* installs and publishes to GitHub Packages.
# GH_TOKEN is expanded from the env at runtime by bun/npm.
# ---
@utegsk:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GH_TOKEN}
